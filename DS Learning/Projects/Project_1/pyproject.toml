[project]
name = "projects"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "moviepy>=2.2.1",
    "numpy>=2.5.1",
    "pandas>=3.0.3",
]

[tool.uv.workspace]
members = [
    "Project2",
]
